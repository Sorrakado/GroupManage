package serviceImpl;

import entity.Group;
import entity.User;
import serviceInterface.addInterface;

public class add implements addInterface {
    @Override
    public boolean add(Group group, User user){


        return true;
    }
}
