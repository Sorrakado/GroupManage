package serviceImpl;

import entity.User;
import serviceInterface.transferInterface;

public class transfer implements transferInterface {
    public boolean transfer(User user){
        return true;
    }
}
