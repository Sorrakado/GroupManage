package serviceImpl;

import entity.User;
import serviceInterface.messageInterface;

public class sendMessage implements messageInterface {
    public boolean sendMessage(User user){
        return true;
    }
}
